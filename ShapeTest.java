class ShapeTest {
    public static void main(String args[])
    {
        Shape Cuboid = new Shape() ;
        Cuboid.area(2,3,4) ;
        Cuboid.area(2,3) ;
        Cuboid.area(2);
    }
}
